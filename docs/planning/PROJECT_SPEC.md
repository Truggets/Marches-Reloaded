# Project Spec — Marches Reloaded

*Trugg Plan · Phase 3 · 2026-07-18*

## 0. Purpose

Marches Reloaded is a private, SRD-legal home for Truman's D&D campaign and the tools around it. Its first component is a web-based **character builder** that lets his friend group create, level, and store fully rules-legal D&D 2024 (SRD 5.2) characters — including multiclassing up to level 10 — each represented by an 8-bit pixel-art avatar. Characters live server-side on Truman's own VPS behind simple accounts, so sheets persist across devices and the group has one shared home. A later component adds the campaign itself (session notes, world/lore, NPCs, house rules). It is not a product and will never be sold; all game-rules content stays inside the Creative Commons SRD.

## 1. Functionality / Jobs-to-be-done

**Who is this for?** Truman (as DM/admin) and a small, fixed group of his friends (players). Private and trusted — not the public. Realistically a handful of accounts.

**What does it need to solve?** Character creation is the friction point for casual players: rules scattered across books they may not own, fiddly math (modifiers, proficiency, spell slots, multiclass rules), and no shared place to keep sheets. Commercial tools gate content behind purchases. The group needs a free, private, SRD-legal builder on Truman's own domain where everyone's characters live together.

**What does it actually do?** (behavior, not tech)

- A friend visits the site, logs in (or registers with an invite code — username + password, no email).
- They start a new character and walk a guided flow: pick species, class, background, generate/assign ability scores, choose skills and proficiencies, pick starting equipment, and (for casters) choose spells — all limited to SRD 5.2 options.
- They can **level the character up to level 10**, gaining the correct features, ability-score improvements/feats, and spells at each level.
- They can **multiclass** — add levels in additional classes, with the builder enforcing multiclass prerequisites, combining spell slots correctly, and granting the right multiclass proficiencies.
- Each character gets an **8-bit pixel-art avatar**.
- The finished character shows as a clean, computed **character sheet** (all derived stats filled in) that can be viewed and printed/exported.
- Characters are **saved to the player's account** on the server; a player sees and edits only their own, while Truman (DM/admin) can see the whole party and reset passwords.

## 2. Scope & Non-Goals

**In scope (builder MVP → v1):** account login (no email), invite-gated registration, DM/admin role, a **content-driven data engine** that reads all game content from "content packs," **SRD 5.2 shipped as the default pack**, an **admin content-pack import** so the owner can load additional content the group owns, guided character creation, leveling to 10, multiclassing, computed character sheet with print/export, per-character 8-bit avatar, server-side storage of characters per user.

**Out of scope / not this project's job:**

- No commercial features — no billing, public sign-ups, or marketing.
- **The distributed/shipped app bundles SRD 5.2 content only.** Non-SRD content (e.g. full 2024 PHB material) is *not* shipped with the app; instead the app provides the machinery for the owner to import content packs into their own private instance. Rationale: owning a book grants use, not a license to redistribute its text — so the app ships the CC-BY SRD and lets the private instance carry whatever its owner loads. See `key-questions.md`.
- Not a virtual tabletop — no maps, initiative/combat tracker, or in-app dice-based play (a simple dice roller is optional polish, not core).
- No leveling past 10.
- No mobile-native app (responsive web is enough).
- The **campaign hub** (session notes/lore/NPCs) is a later phase of Marches Reloaded, not part of the builder MVP.

## 3. Technical Design

Full research and rationale in `tech-research-report.md`. Summary:

**Stack:** React + TypeScript + Vite + Tailwind (frontend/rules engine) · Node + thin JSON API (Express) · SQLite · minimal session auth (argon2 + HTTP-only cookie, no email) · self-hosted on the existing Hostinger VPS behind Caddy via the proven git-deploy pipeline · DiceBear Pixel Art (CC0) for avatars · **build from scratch** (existing builders are all 2014-edition; this is 2024/SRD 5.2). SRD data is a **self-owned bundled JSON dataset** (seeded from Open5e `srd-2024` + the SRD 5.2.1 markdown), not a live API.

**Architecture / system design:**

- **Client (React SPA)** holds the whole character-building experience and the **rules engine** — all derived-stat computation (modifiers, proficiency, AC, HP, spell slots, multiclass math) happens client-side against the bundled SRD dataset. This keeps the server thin and the rules logic in typed code where bugs are catchable.
- **Content-pack engine (core).** All game content is expressed as **content packs** — a defined JSON schema for species, classes + per-level features, subclasses, backgrounds, feats, spells, equipment. Every entry carries a `pack`/`source` id. **SRD 5.2 is the built-in default pack** (bundled, CC-BY). The engine is content-driven: the rules logic reads packs, nothing about a specific class is hardcoded. An admin can **import additional packs** into their instance; a character records which pack each choice came from so sheets stay reproducible. This makes "use content we own" a first-class capability of the owner's private instance without the shipped app redistributing non-SRD text.
- **Server (Node/Express JSON API)** does only three things: authenticate (register/login/logout, session cookies), store/retrieve **characters** (per-user CRUD, character saved as a JSON document), and admin actions (password reset, party view). No game rules live on the server.
- **Database (SQLite):** `users` (id, username, argon2 hash, is_admin), `sessions`, `characters` (id, owner_user_id, JSON blob, timestamps), `invites` (codes for gated registration). Nightly file backup.
- **Auth flow:** invite-code-gated registration → argon2-hashed password → signed HTTP-only session cookie → middleware gates character write/read to the owner; `is_admin` unlocks party view + password resets.
- **Deploy:** standalone repo → git-based `deploy.sh` (mirroring the dashboard) → systemd service as an unprivileged user → Caddy terminates TLS and serves the static React build + reverse-proxies `/api` to Node → own subdomain (e.g. `marches.therinkinc.com`). Production push and deploy each require Truman's explicit per-action go-ahead.

**Data flow (build a character):** player logs in → client loads bundled SRD data → guided wizard collects choices, rules engine computes the sheet live → on save, the client POSTs the character JSON to `/api/characters` → server stores it against the user in SQLite. Viewing/leveling re-hydrates that JSON into the client engine.

## 4. Milestones

Ordered so each depends only on those before it. Early ones precise; later ones intentionally loose.

| # | Milestone | What "done" looks like | Status |
|---|---|---|---|
| M0 | Skeleton + deploy | Empty app builds and serves at the target URL on the VPS via the git-deploy pipeline; TLS working. | **Done** (2026-07-19) — live at https://marches.therinkinc.com, `/api/health` returns 200 via Caddy reverse proxy, TLS auto-provisioned. |
| M1 | Accounts & login | Invite-gated register, log in/out, hashed passwords (no email), DM/admin role, admin password reset. | **Done** (2026-07-19) — verified live at https://marches.therinkinc.com: invite-gated register/login/logout, `/api/auth/me`, admin (`truman`) reset-password, 403 for non-admins. |
| M2 | Content-driven data engine + SRD 5.2 pack | Engine reads all content (species, classes/subclasses, backgrounds, feats, spells, equipment) from a **content-pack format**; **SRD 5.2 ships as the default pack**; data is queryable and pack-scoped. This is now core architecture, not a late add-on. | **Done** (2026-07-18) — full SRD 5.2.1 pack parsed from `downfallx/dnd-5e-srd-markdown` (pinned commit `1b4b99d`, CC-BY 4.0) via deterministic build scripts in `app/data/build/`: 12 classes (20 levels + 1 subclass each), 9 species, 4 backgrounds, 17 feats, 339 spells, 174 equipment items. `app/data/build/verify-pack.js` passes (counts reconciled, known-value spot checks). Typed query layer at `app/data/index.ts`, consumed by the client via a Vite alias (`@data`); `npm run build` in `client/` succeeds. CC-BY attribution rendered in-app (`Credits.tsx`). |
| M2b | Content-pack import (admin) | Admin/DM can import an additional content pack into their instance (validated against the pack schema) so the group can use content it owns; characters record which pack each choice came from. | **In progress** — v1 scope locked to feats/backgrounds/species/equipment (`docs/planning/m2b-execution-plan.md`); subclasses/monsters/hazards/magic items deferred to issues #21/#22. Phase 1 (feats, 58 vault entries) shipped 2026-09-08 (`bd46f20`). Phase 2 (backgrounds, 42 entries) shipped 2026-09-09 (`560a8b1`). Phase 3 (species, 18 entries) + Phase 4 (equipment: 38 weapons/13 armor/18 gear, plus new structured `damage`/`mastery`/`ac`/`strength`/`stealth` fields on `EquipmentEntry`, applied to both the bundled and imported parsers) shipped 2026-09-09 (`9f38c3a`/`2039f29`/`100fafe`). All four phases share one `POST /api/admin/packs/import` endpoint with per-content-type merge-on-conflict (importing one type never wipes another already stored under the same pack id) and a 4-textarea `AdminPackImportPage.tsx`. Not yet run against production — no admin has actually pasted real vault JSON into a live instance yet, verified via unit tests + parser runs against the real vault files locally (never committed) instead. |
| M3 | Level 1 character creation | Full happy-path single-class L1 build (species/class/background/abilities/skills/equipment/spells), saved to the player's account. | **Done** (2026-07-19) — live at https://marches.therinkinc.com. Server: `characters` table (additive migration) + `/api/characters` CRUD behind `requireAuth` with owner/admin checks. Client: 7-step wizard (Class → Origin → Ability Scores [roll or manual entry] → Skills → Equipment → Spells → Name) built on the M2 `@data` query layer, storing choices (not computed stats) in a schema shaped to survive M6 multiclassing (`classes: []` array from day one). Verified live in production with two full happy paths: a caster (Human Sage Wizard 1, auto-rolled abilities, +1/+1/+1 background increase) and a non-caster with a 3-option equipment class (Dwarf Soldier Fighter 1, manually-entered ability scores, +2/+1 background split) — both via register → create → list → delete against the real deployed API. One rules bug caught and fixed during this pass: the Spells step was offering the class's entire spell list (up to 9th level) instead of only 1st-level spells choosable at character level 1. |
| M4 | Character sheet view | Read-only computed sheet (modifiers, proficiency, AC, HP, spell slots), printable/exportable. | **Done** (2026-07-19) — live at https://marches.therinkinc.com. Standalone unit-tested rules engine (`client/src/engine/computeSheet.ts`, 13/13 tests via `vitest`) reconstructs final numbers from M3's stored choices (handles both background-increase shapes), plus `CharacterSheetPage.tsx` at `/characters/:id`. Armor Class is armor-aware: re-derives the chosen equipment option's item text and matches it against `equipment.json`'s armor/shield entries for a real AC formula (flat, Dex-capped, or unarmored fallback, shield additive) — not just flat 10+Dex (known gap: doesn't special-case Barbarian/Monk Unarmored Defense). Verified live in production against two real saved characters: a Fighter (Chain Mail, flat AC) and a Cleric (Chain Shirt + Shield, Dex-capped AC + shield bonus, spell slots) — HP, AC, saving throws, and skill proficiencies all matched hand-calculation exactly on both, confirming the earlier M3 spell-level-filter fix holds. Print CSS confirmed correctly wired; JSON export confirmed working end-to-end (downloaded and inspected). |
| M5 | Leveling to 10 (single class) | Level-up flow grants correct features, ASI/feat choices, and spells per level up to 10. | **Built, verified locally** (2026-07-19) — engine (`computeSheet.ts`) generalized to accept level 1-10 (`proficiencyBonus`, `hitPoints`, `spellSlots`, plus new `featuresForLevel`/`isAsiLevel`), 21/21 vitest passing. New `LevelUpPage.tsx` stepper at `/characters/:id/level-up` walks level-by-level, showing new features, a General-feat picker at ASI levels (with ability-increase sub-choice, capped at 20), and spell/cantrip pickers filtered to the newly-unlocked max spell level. Fixed a real M4 bug caught during this milestone: Dwarven Toughness (+1 HP/level) was never applied — `hitPoints()` now takes `speciesId` and applies it. Verified live in local dev with two full level-1→5 passes: a Dwarf Fighter (HP 13→49 matching hand-calc including Dwarven Toughness, proficiency +2→+3, ASI applied to Strength) and a Human Wizard (HP 9→37, cantrips 3→4, spell slots growing correctly through 3rd level, ASI split across two abilities including an at-cap edge case). Also caught and fixed a second bug during this same verification pass: the character sheet was only rendering the original wizard-step spell picks, not spells added via level-up — `CharacterSheetPage.tsx` now merges `levelUps[].spellsAdded` into the displayed cantrip/prepared lists. Known, accepted gaps carried forward: subclass choice is not an interactive step (shows the generic "X Subclass" feature-table label only), Fighting-Style-style L1 choices are not retrofitted into M3, no spell re-preparation/swapping, Paladin/Ranger spell attack/DC math stays out of scope (same reason as M4). **Pushed and deployed to production (2026-07-19)** — release `20260719171724-690fcc0` live at https://marches.therinkinc.com. Verified via `Brondra Stonefaith` (Dwarf Cleric 5): proficiency +3, HP 43, spell slots through 3rd level all correct in prod. |
| M6 | Multiclassing | Add levels in additional classes with prerequisite checks, combined spell slots, and multiclass proficiencies. | **Built, verified locally** (2026-07-19) — `CharacterData.classes` is now a real array (backward-compatible; existing saves already length-1). Engine (`computeSheet.ts`) gained `totalCharacterLevel`, `proficiencyBonusMulticlass` (total-level lookup), `hitPointsMulticlass` (max-hit-die applies once, at level 1 of the character's very first class only — every other level, in any class, is fixed), `combinedCasterLevel`/`combinedSpellSlots` (full-caster levels summed, plus Paladin/Ranger levels **summed together and rounded up once** — confirmed against the SRD 2024 text directly rather than assumed from memory, and specifically NOT each half-caster class rounded independently before summing, which a second-opinion review caught as a real bug during this same session: Paladin 1/Ranger 1 must combine to caster level 1, not 2), `warlockPactMagic` (Warlock Pact Magic kept as a fully separate pool, never folded into the combined table), and `canMulticlassInto` (13-in-primary-ability prerequisite, parsing compound `primaryAbility` strings like "Strength or Dexterity" as OR and "Dexterity and Wisdom" as AND, checked against the new class and every current class). `multiclassMath.test.ts` (14 tests) covers full+full slot combination, the half-caster round-up edge case, the both-half-casters-combine-before-rounding case, the SRD worked example (Ranger 4/Sorcerer 3 = combined level 5), Warlock separation, ASI-per-class-level timing (Fighter 2/Wizard 2 has zero ASIs despite total level 4), the HP-first-level-only rule, and both OR/AND prerequisite forms — all 51 client tests pass (37 existing + 14 new), `tsc -b` and `npm run build` both clean. Real data-layer bug fixed along the way: `multiclassTraitsGranted` was empty for 7 of 12 classes (Fighter, Cleric, Monk, Ranger, Rogue, Sorcerer, Warlock) due to a parser regex hardcoded to the `•` bullet character while those classes' source markdown uses plain `-` bullets — fixed in `data/build/parse-classes.js`, pack rebuilt, and a non-empty assertion added to `data/build/verify-classes.js` to catch any regression. `LevelUpPage.tsx` gained a first "which class advances" step (level an existing class, or multiclass into a new one — new-class options filtered live through `canMulticlassInto`); adding a class grants no starting equipment (per SRD) and only a spell-pick step if the new class is a caster; multiclass proficiencies (from the now-fixed `multiclassTraitsGranted` text) are shown as display-only info, consistent with the sheet's existing non-enforcement of species-trait mechanics. `CharacterSheetPage.tsx` now shows all classes ("Wizard 8 / Cleric 1"), combined proficiency bonus/HP, combined spell slots plus a separate Pact Magic row when applicable, and per-class cantrip/prepared-spell sections. Verified live in local dev in two passes against the persistent `QA-Wizard` test character: (1) a non-caster multiclass (Wizard→Fighter) — the eligible-classes list correctly showed only Dexterity/Strength-13-satisfying classes, HP came out to exactly max-die (Wizard) + fixed (Fighter) = 16, and the sheet correctly filtered Fighter out of the spellcasting section entirely; (2) a caster+caster multiclass (Wizard 8→Cleric 1, after leveling Wizard to 8 and using its ASI to bring Wisdom to 13) — the eligible-classes list correctly grew to 6 classes as abilities improved, Cleric's own independent level-1 cantrip/spell picker appeared and was completed, HP added exactly the fixed (non-max-die) value, and the final sheet's combined spell-slot row (L1:4/L2:3/L3:3/L4:3/L5:1) matched the real bundled pack's level-9 full-caster table exactly, with separate correctly-isolated Wizard and Cleric cantrip/prepared-spell sections. (Process note: this local Windows dev environment had several stale `node --watch`/vite processes competing for the same ports across earlier sessions, which briefly caused confusing "state reverted" symptoms — cleaned up via `Get-CimInstance Win32_Process`; worth checking for before trusting local dev state in a future session.) Known, accepted scope cuts (stated, not silently dropped): multiclass proficiencies are display-only (no weapon/armor mechanical enforcement — the sheet has no attack-bonus modeling to enforce against, a carried-forward M4/M5 gap); third-caster subclasses (Eldritch Knight/Arcane Trickster) excluded since subclass choice is still non-interactive; Paladin/Ranger spell attack/DC math stays out of scope (same reason as M4/M5); a pre-existing M5 gap noticed but not fixed here (out of M6 scope) — the level-up spell-pick step's "already known" filter only checks the in-progress session's picks plus the original creation spells, not spells learned in earlier, already-saved level-up sessions, so a spell could in principle be offered again in a later session. **Pushed and deployed to production (2026-07-19)** — release `20260719180356-def13a0` live at https://marches.therinkinc.com. Verified live via a real multiclass save on `Brondra Stonefaith` (Dwarf Cleric 5 → Cleric 5 / Druid 1, both Wisdom-gated): eligible-classes list correctly showed only Druid, HP added exactly the hand-calculated fixed value (43→51, including Dwarven Toughness), and the sheet correctly showed the combined proficiency bonus (+3 at total level 6), combined spell slots, and separately-isolated Cleric/Druid cantrip and prepared-spell sections. |
| M7 | 8-bit avatars | Each character has a pixel-art avatar (approach chosen in Phase 4); shown on sheet and in character list. | **Built, verified locally** (2026-07-19) — deterministic, fully client-side pixel-art avatars via `@dicebear/core` + `@dicebear/pixel-art` (CC0 1.0), seeded by the character's stable numeric `id` (no schema/server change — no live network call, no ongoing cost, matching the tech-research-report's locked decision). New `client/src/avatar.ts` (pure `avatarDataUri(seed)` function, unit-tested for determinism and distinctness) and `client/src/CharacterAvatar.tsx` (the `useMemo`-wrapped component — confirmed `toDataUri()` is synchronous in 9.4.3, so no loading state needed), wired into both `CharacterListPage.tsx` (48px, next to each character's name) and `CharacterSheetPage.tsx` (96px, in the header). Depends directly on `@dicebear/pixel-art` rather than the `@dicebear/collection` barrel (which bundles 29 unused styles) — confirmed via the built output that this added only 37.6 kB (8.8 kB gzip) to the production bundle, not hundreds of kB. Confirmed no CSP is set anywhere in this stack (checked both the Express server and the deployed Caddyfile) before assuming a `data:` URI `<img>` would render unblocked. Optional CC0 attribution line added to `Credits.tsx` alongside the existing SRD attribution. New feature doc at `docs/avatars/README.md`. 40/40 client tests pass, `tsc -b` and `npm run build` both clean. Verified live in local dev: five real test characters (QA-Wizard, QA-Rogue, QA-Fighter, QA-Warlock, QA-Cleric) each show a visibly distinct avatar in the list, and QA-Wizard's sheet-header avatar (96px) matches its list avatar (48px) exactly — same seed, same character, everywhere it's shown. **Pushed and deployed to production (2026-07-19)** — release `20260719201519-10cb47a` live at https://marches.therinkinc.com. See incident note below for the deploy-script fix that preceded this deploy; avatar feature confirmed live (visible pixel-art avatar + DiceBear CC0 attribution in the footer). |

**Incident (2026-07-19):** deploying M7 triggered a real, if brief, production outage. `deploy.sh`'s release-pruning step used `ls -t` (sort by mtime) to decide which old releases to delete — but every release directory is populated via `rsync -a` from the same persistent `$REPO_DIR` working copy, which preserves that source's mtimes, so release directories accumulate identical/stale mtimes over time and mtime order stops matching creation order. The prune step mistook the release it had *just deployed* for the oldest one and deleted it, leaving the `current` symlink pointing at nothing — the whole site (not just the avatar feature) 404'd. Diagnosed live on the VPS (confirmed via `ls -la` that all release directories shared an identical `05:03` mtime), recovered by rolling back explicitly by release name (`rollback.sh` with no argument also picks the wrong target for the same mtime reason — the fix (commit `014e566`) sorts release directory names, which are timestamp-prefixed and sort correctly lexicographically, instead of relying on `ls -t`.

**Resolution (2026-07-19):** the git-level fix alone wasn't sufficient — the running `deploy.sh`/`rollback.sh` on the VPS at `/opt/marches-reload/scripts/` are standalone copies that the release pipeline never overwrites (nothing copies `scripts/` out of a release into that path). Patched both files directly on the VPS (originals backed up as `.bak-20260719`), confirmed `bash -n` syntax-valid, and dry-ran the new prune logic read-only against the real release directory before trusting it live. Then retried the M7 deploy as the canary: it correctly pruned the oldest release (`20260719050319-b24ad3d`, an M3-era release) rather than the one it had just deployed, `current` resolved correctly, and the live site loaded with the M7 avatar visible. Incident fully closed.

**QA bug-fix pass (2026-07-19):** an independent manual QA pass (all 12 SRD classes created and leveled 1→5 through the real UI) surfaced 5 findings. Triaged: 2 were already-documented, deliberately-accepted scope cuts (Unarmored Defense not modeled for Barbarian/Monk; subclass choice non-interactive) — not regressions, left as-is. 1 (class features not persisted on the saved sheet, only shown transiently during level-up) is a product decision, not a defect — left for Truman to scope later if wanted. 2 were real bugs, both fixed this session:
- **Spell-count over-grant (M5):** `LevelUpPage.tsx`'s prepared-spell count was derived from the *change in total spell slots* between levels, not the class's actual `preparedOrKnown` value — for Bard/Druid these grow at different rates, so the stepper over-offered (e.g. QA-Bard ended up with 11 prepared spells at L5 instead of the correct 9). Fixed by having `spellSlots()` (`computeSheet.ts`) return the real `preparedOrKnown` field (already present in `classes.json`/`schema.ts` but never read) and using its level-to-level delta instead of the slot-total delta. Regression test added (`computeSheet.test.ts`) proving the old logic would say Druid L2→L3 grants +3 when the real number is +1. Existing over-granted data on the dev-only QA-Bard/QA-Druid test characters was left as-is (not production data, no players affected yet).
- **Human bonus-choice traits never implemented (M3):** `species.json`'s Human entry has two open-ended bonus traits — Skillful (one bonus skill proficiency) and Versatile (one bonus Origin feat) — that were pure flavor text with no wizard step behind either. New `StepSpeciesBonus.tsx` step, inserted into the creation wizard (data-driven off the species' own trait names, not hardcoded to `speciesId === 'human'`, so any future species with the same trait names gets the step automatically), lets the player pick a bonus skill (excluding ones already granted by class/background) and an Origin feat (`alert`/`magic-initiate`/`savage-attacker`/`skilled`). New `originFeatId?: string` field on `CharacterData`; bonus skill folds directly into `skillProficiencies`. Sheet displays the chosen feat under the Species section. Known, accepted shallow spot matching the existing convention: picking Magic Initiate here (like a background's fixed Magic Initiate feat) doesn't have a spell-list sub-picker — just the feat name, same limitation the background-granted version already has. Verified live end-to-end with a scratch Human Fighter: species-bonus step appeared, Stealth (bonus skill) and Skilled (bonus feat) were both chosen and correctly displayed on the saved sheet with the right final skill count.

42/42 client tests pass (40 existing + 2 new regression tests for the spell-count fix; the species-bonus fix was verified via manual browser walkthrough rather than a new automated test), `tsc -b`/build clean. **Pushed and deployed to production (2026-07-19)** — release `20260719204908-df6daad` live at https://marches.therinkinc.com, confirmed via the live character list and `/api/health`.
| M8 | Polish & party view | DM party view (see all characters), edit/delete own character, error handling, responsive cleanup, optional dice roller. | **Built, verified locally** (2026-07-19) — scope confirmed with Truman before building: "edit" = rename only (not a full re-edit wizard), dice roller skipped (explicitly optional in the DoD). **Party view:** new admin-only `GET /api/admin/characters` (server/src/routes/admin.js, reusing the existing `requireRole("admin")` middleware) joins every character with its owner's username; new `PartyViewPage.tsx` at `/party` (nav link shown only when `user.isAdmin`, matching the existing "Invite codes" pattern). Verified both directions live: as a (throwaway) admin account the page lists every character across every user; a non-admin session's direct `GET /api/admin/characters` request returns 403 (checked directly, not just that the nav link is hidden). `CharacterSheetPage.tsx`'s "Level Up" button is now hidden when the viewer isn't the character's owner (`user.id !== character.ownerId`), so an admin browsing via the party view gets a read-only sheet, not an accidental edit path. **Rename:** inline rename on `CharacterListPage.tsx`, resending the character's full existing `data` alongside the new name (the PUT endpoint has no partial-patch mode — sending just `{name}` would 400, and `{name, data:{}}` would clobber the character). Verified: renamed a real character, reloaded, name persisted and `data` was untouched. **Error handling:** new `ErrorBoundary.tsx` — a class component (function components can't implement `componentDidCatch`) — wraps the whole app in `main.tsx`; verified by deliberately throwing in a child component and confirming the fallback UI rendered instead of a blank white screen (then reverted the test throw). New catch-all `NotFoundPage.tsx` at route `*`; verified a bad URL shows it instead of a blank screen. **Responsive cleanup:** checked at ~500px and ~768px viewport widths (browser automation's `resize_window` landed a bit wider than the requested 375px, but well within "narrow phone" range); found and fixed one real overlap — the fixed-position, purely-decorative `WilburCompanion` was floating on top of real sheet content (e.g. the Hit Points card) at narrow widths, so it's now hidden below 640px via a media query rather than obscuring anything. The new party view's card layout already wraps cleanly at both widths with no horizontal overflow. 42/42 client tests pass, `tsc -b`/build clean. All manual verification used throwaway test accounts created directly in the local dev DB (never touching production or the shared QA account), deleted immediately after each check. **Pushed and deployed to production (2026-07-19)** — release `20260721213337-36d64dc` live at https://marches.therinkinc.com, confirmed via the live character list (rename buttons visible) and `/api/health`. |
**Post-M8 bug-fix pass (2026-07-21):** Truman reported 16 issues from real play. Investigated each against the actual code before touching anything — several turned out to be the *same* underlying gap (class features never persisted on the sheet explains 3 separate complaints), several were already-documented accepted scope cuts resurfacing (Unarmored Defense, no spell re-preparation, no mechanical proficiency/attack-bonus system), and a few were genuine data-completeness or larger feature gaps (only 2 curated "General" feats exist in the bundled pack; no Wizard spellbook-vs-prepared distinction; Origin feats that grant spells don't actually grant them). Truman chose to fix the isolated bugs plus one self-contained feature build now; the rest stay open for a future scoping decision. Fixed and deployed this pass:
- **Cancel character creation** — the Back button at step 1 now navigates to `/characters` (labeled "Cancel") instead of just being disabled with no way out.
- **Reroll mode lost on step revisit** — `StepAbilities.tsx` re-initialized to manual-entry mode every time the step remounted (e.g. after going Back then Next again), even if the player had used "Roll for me." Added a `rollMode` field to `AbilityScoresData` so the step remembers which mode was actually used.
- **INT/CON sheet boxes clipped text** — the longest ability names overflowed their grid cell at the default label size/padding. Matched the Saving Throws section's already-correct treatment (`text-[0.55rem]`, tighter padding).
- **Prepared spells showed no level or description** — the data already had both (`SpellEntry.level`/`.description`); `CharacterSheetPage.tsx` just wasn't rendering them. Now shows each spell's level and full text.
- **Level-up re-offered spells from an earlier, already-saved level-up session** — `LevelUpPage.tsx`'s "already known" filter only checked original creation-time spells plus the current in-progress session, never `data.levelUps` from prior sessions. Fixed by mirroring the same filter `CharacterSheetPage.tsx`'s `spellsForClass()` already used correctly. Verified with a real two-session test: leveled a Wizard 1→2 picking Mage Armor, saved, started a fresh 2→3 session, confirmed Mage Armor no longer appeared as an option.
- **Class Features never shown on the saved sheet** (covering the Lay on Hands / Rage / general-case complaints) — `featuresForLevel()` was computed and used only transiently during the level-up stepper. New "Class Features" section on `CharacterSheetPage.tsx` aggregates every feature from level 1 up to the character's current level, per class.

42/42 client tests pass, `tsc -b`/build clean. All manual verification (including the two-session level-up test) used throwaway accounts/characters created directly in the local dev DB, deleted immediately after each check. **Pushed and deployed to production (2026-07-21)** — release `20260721222224-718711a` live at https://marches.therinkinc.com, confirmed against a real character (`Brondra Stonefaith`, Cleric 5/Druid 1): Class Features section correctly lists both classes' real features, and prepared spells show level + description.

**Still open, not fixed this pass** (Truman to decide priority later): Origin feats that grant spells (Magic Initiate) don't actually add them — medium; no Spell Attack bonus/Spell Save DC anywhere — small-medium; Weapon Mastery — large, needs an attack-bonus system that doesn't exist; mechanical proficiency enforcement — large, same root as Weapon Mastery, already an accepted scope cut; Unarmored Defense — already an accepted scope cut; Wizard spellbook vs. prepared-spells distinction — large, data-model gap; no spell re-preparation — large, already an accepted scope cut, same root as the spellbook gap; level-up feat picker only offers 2 General feats because the bundled data pack only *has* 2 — large, needs re-curating `data/feats.json` from source, same class of gap as the M6 bullet-regex bug.

**GitHub issue reconciliation (2026-07-21):** a playtester (GitHub user `chasinwaves`) had independently filed 11 issues on `Truggets/Marches-Reloaded` covering nearly this exact bug list — presumably the origin of Truman's report. Closed #1, #7, #8, #9 (fully fixed, commit `718711a`) with fix-reference comments. #11 ("Missing class features") was only partially resolved by the Class Features section — the feature *name* now shows for Unarmored Defense but the AC math still isn't computed — so split that gap into new issue #12 and closed #11. Opened #13 for the feat-data curation gap. Remaining open: #2 (Origin feat spells), #3 (Weapon Mastery), #4 (Proficiencies/Languages), #5 (Wizard spellbook), #6 (re-prepare spells), #10 (spell attack/DC), #12 (Unarmored Defense AC math), #13 (feat data curation) — a clean 1:1 match with the "still open" list above.

**Wilbur companion building-advice feature (2026-07-21):** new `WilburTip.tsx` component renders an inline speech-bubble (not the fixed-position decorative `WilburCompanion` — that one stays purely cosmetic and hidden below 640px; this one carries real content so it renders in normal document flow at every width) showing contextual building advice on every step of the character-creation wizard. Data-driven where real content exists — Class step shows the selected class's `primaryAbility`; Origin step shows the selected species' trait names and the selected background's granted skills + feat (e.g. "Sage grants proficiency in Arcana and History, plus the Magic Initiate (Wizard) feat"); Abilities step reminds the player to prioritize their class's primary ability. Remaining steps (Skills, Species Bonus, Equipment, Spells, Name) get simple static tips rather than fabricated deep game content. Verified live: selecting Wizard showed "Wizard's primary ability is Intelligence..."; selecting Human + Sage showed "Human grants: Resourceful, Skillful, Versatile. Sage grants proficiency in Arcana and History, plus the Magic Initiate (Wizard) feat." 42/42 tests pass, `tsc -b`/build clean.

**Follow-up same day — describe the last-selected feat/spell:** extended `wilburTipFor()` to show the *actual benefit/description text* of whatever the player most recently picked, not just its name, per Truman's request ("so they know what it does"). Origin step now appends the background's feat's full `benefit` text (matched from `backgroundEntry.feat`, e.g. "Magic Initiate (Wizard)", by stripping the parenthetical and looking it up in `feats.json` by name). Species Bonus step shows the selected Origin feat's benefit text directly (via `originFeatId`). Spells step tracks whichever cantrip/prepared spell was most recently added (new `lastSpellId` state, diffed in the `onChangeCantrips`/`onChangePrepared` wrappers) and shows its level + full description. Verified live for all three: selecting the Skilled feat, Fire Bolt, and Magic Missile each correctly updated Wilbur's text to that item's real benefit/description. **Same-day follow-up — made it collapsible:** `WilburTip.tsx` now truncates any tip over 120 characters with a "Show more"/"Show less" toggle, so long benefit text (Magic Initiate's, etc.) no longer pushes the Next button down by default. **Pushed and deployed to production (2026-07-21)** — release `20260721231402-59559b8` live at https://marches.therinkinc.com, confirmed via the live Class step (truncated tip + working toggle) and `/api/health`.
| M9 | Campaign hub (future) | Session notes, lore, NPCs, house rules — separate later phase of Marches Reloaded. | Placeholder |
| M10 | Content-authoring UI (future) | In-app editor to *create/edit* content packs (vs. importing a pre-made file at M2b). Nice-to-have polish. | Placeholder |
| M11 | Combat sandbox ("target dummy" mode) | Single-player scratch space: test a saved character's attacks against a small bundled set of SRD monsters on one open grid, no persistence/multiplayer/map-authoring. Ships caster-only first (spell attacks via existing `spellcastingInfo`); martial-character support needed structured weapon data + a `weaponAttack()` engine function (M2b Phase 4 + issue #3's sandbox-relevant half). See `docs/planning/content-ux-and-sandbox-plan.md`. | **Caster + martial v0 built and browser-verified** (2026-09-09, `docs/planning/m11-sandbox-v0-plan.md` + `docs/planning/m11-phase2-weapon-attack-plan.md`). Caster half: new `MonsterEntry` schema category + 10 curated low-CR monsters (`data/build/parse-monsters.js`), `resolveSpellAttack`/`resolveMonsterAttack` turn-resolution with a curated 9-cantrip damage lookup (`client/src/engine/sandbox.ts`/`spellDamage.ts`) — verified live with a Cleric 1/Wizard 4 multiclass caster, catching and fixing two real multiclass bugs (a `classes[0]`-only validation gap, first-match-only caster-class selection) before push. Martial half (Phase 2): `parseWeaponsFromEquipmentChoice()` turns a character's starting-equipment letter into concrete weapon entries (`client/src/engine/equipmentAttack.ts`, no schema change needed — ability-for-attack derives from each weapon's existing `properties` string), `resolveWeaponAttack()` mirrors the caster resolvers, both wired into `CombatSandboxPage.tsx` behind the same picker/log UI. Explicitly scoped separately from issue #3's literal ask (a Fighting Style/Weapon Mastery *selection UI* in the wizard, untouched here) — mastery names show as attack-log flavor text only, never mechanically applied. Review caught and fixed a real bug before push: weapon damage initially omitted the ability modifier the attack roll already included. Browser-verified with a real Fighter 3/Rogue 2 multiclass across three fixtures — a STR-build with a non-Finesse weapon (Greatsword, `+6`/`2d6 + 3 Slashing`), a DEX-build with a Finesse weapon and a bow (Scimitar/Longbow, both correctly DEX-derived), and a gold-only-equipment pick (friendly empty state, not a crash). Not in scope for v0: actually applying Weapon Mastery rules, Fighting Style effects, two-weapon fighting, range/reach/cover. |

## 5. Key Risks / Assumptions

- **SRD 5.2 data completeness.** Assumes the free datasets (Open5e `srd-2024`, 5e-bits `/api/2024`) cover the 2024 character-creation rules well enough (species traits, class features per level, backgrounds, feats, spells). Gaps here directly hit M2–M5 — to be verified in Phase 4.
- **Multiclassing correctness is the hardest part.** The rules engine (prerequisites, shared spell-slot table, proficiency-on-multiclass) is the biggest source of subtle bugs. Budget accordingly; consider it the milestone most likely to slip.
- **No-email password recovery.** Forgotten passwords require manual admin reset — acceptable for a private group but a real operational assumption.
- **Avatar asset licensing.** Pixel-art assets must be owned or permissively licensed (CC0/MIT/CC-BY); to be locked down in Phase 4.
- **Open-page + accounts.** Registration must be gated (invite code) so a leaked URL doesn't let strangers create accounts.
- **Legal.** Everything shipped stays inside SRD 5.2 (CC-BY 4.0) with attribution; Truman's own campaign content is unrestricted.
